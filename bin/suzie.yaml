suzie:
    connection:
        host: '%env(DB_HOST)%'
        user: '%env(DB_USER)%'
        pass: '%env(DB_PASS)%'
        port: '%env(DB_PORT)%'
        dbname: '%env(DB_NAME)%'
        charset: '%env(DB_CHARSET)%'